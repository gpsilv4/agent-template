{
  "name": "ci-guard-probe",
  "version": "0.0.0",
  "private": true,
  "description": "TEMPORARY probe to exercise ci.yml per-step guards in Actions. Delete with the branch.",
  "scripts": {
    "lint": "echo GUARD-PROBE: lint ran",
    "build": "echo GUARD-PROBE: build ran",
    "test:unit": "echo GUARD-PROBE: test:unit ran"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
